# Arc mainnet services. Copy to .env.mainnet and fill in values (KRYON_NETWORK=arc-mainnet).
# Names only; no values are provided on purpose.

# ── Network ─────────────────────────────────────────────────────────────────
# One of: arc-mainnet | arc-testnet | arc-local   (read by lib/chain/networks.ts)
KRYON_NETWORK=
# Comma-separated provider RPC URLs, tried in order; the public RPC is appended last.
ARC_RPC_URLS=
# Path to a deployment record written by kryon-protocol/evm/script
# (evm/deployments/<network>.json). When set, the CONTRACT_* variables are ignored.
KRYON_DEPLOYMENT_FILE=

# ── Contract addresses (only if KRYON_DEPLOYMENT_FILE is unset) ─────────────
CONTRACT_VAULT=
CONTRACT_ENGINE=
CONTRACT_ORDER_GATEWAY=
CONTRACT_ORACLE_ADAPTER=
CONTRACT_LIQUIDATION=
CONTRACT_INSURANCE=
CONTRACT_RISK_PARAMS=
CONTRACT_FEE_ROUTER=
CONTRACT_TIMELOCK=

# ── Database (server-side only) ─────────────────────────────────────────────
# e.g. postgresql://USER:PASSWORD@HOST:5432/kryon?sslmode=require
DATABASE_URL=
DIRECT_URL=

# ── Public URLs ─────────────────────────────────────────────────────────────
# e.g. https://app.<APP_DOMAIN> and wss://ws.<APP_DOMAIN>
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_WS_URL=

# ── Service keys ────────────────────────────────────────────────────────────
# Each Arc service loads its own 32-byte hex key with serviceAccount(<VAR>)
# (lib/chain/clients.ts). Never share a key between services, and never
# between two processes of the same service: the nonce belongs to the key.
# Matcher: MATCHER_OPERATOR_KEY, one per shard (see below).

# ── Indexer (scripts/state-indexer.ts) ──────────────────────────────────────
# Deployment block; used only when no cursor exists yet.
INDEXER_START_BLOCK=
# Optional: idle poll interval in ms and max blocks per getLogs (both default 2000).
INDEXER_POLL_MS=
INDEXER_MAX_WINDOW=

# ── Matcher (scripts/matcher-service.ts) ────────────────────────────────────
# One process per shard: one operator key, one set of markets. Two matcher
# processes must never share a key — TxSender owns the nonce for its key.
# Market ids this shard matches, comma separated (e.g. 2,3). Required.
MATCHER_MARKETS=
# 32-byte hex private key holding OPERATOR_ROLE on the gateway.
MATCHER_OPERATOR_KEY=
# Optional: shard name for logs (defaults to the market list).
MATCHER_SHARD=
# Optional: tick interval in ms (default 1000).
MATCHER_INTERVAL_MS=
# Optional: fill count below which an InsufficientBatchGas resize gives up (default 1).
MATCHER_MIN_BATCH_FILLS=
# Optional: ms before recovery judges a pending fill with no TxJob (default 60000).
MATCHER_ORPHAN_GRACE_MS=
# Optional: port for GET /health; unset disables the endpoint.
MATCHER_HEALTH_PORT=

# ── LEGACY (removed in Phase 3/4) ───────────────────────────────────────────
# Still read by legacy chain code (lib/stellar/**, config/, older scripts) so
# local builds keep working. Do not add new uses.
NEXT_PUBLIC_STELLAR_NETWORK=
NEXT_PUBLIC_STELLAR_RPC_URL=
NEXT_PUBLIC_STELLAR_PASSPHRASE=
NEXT_PUBLIC_STELLAR_HORIZON_URL=
NEXT_PUBLIC_CONTRACT_GOVERNANCE=
NEXT_PUBLIC_CONTRACT_ORACLE_ADAPTER=
NEXT_PUBLIC_CONTRACT_VAULT=
NEXT_PUBLIC_CONTRACT_ENGINE=
NEXT_PUBLIC_CONTRACT_ORDER_GATEWAY=
NEXT_PUBLIC_CONTRACT_INSURANCE=
NEXT_PUBLIC_CONTRACT_LIQUIDATION=
NEXT_PUBLIC_CONTRACT_RISK=
NEXT_PUBLIC_ASSET_NATIVE_XLM=
NEXT_PUBLIC_ASSET_USDC=
NEXT_PUBLIC_USDC_ISSUER=
NEXT_PUBLIC_ACTIVE_MARKETS=
REFLECTOR_GUARD_ENABLED=
REFLECTOR_DIVERGENCE_HALT_BPS=
REFLECTOR_CONTRACT_ID=
NEXT_PUBLIC_MAINNET_KEEPERS_LIVE=
NEXT_PUBLIC_TESTNET_KEEPERS_LIVE=
DATABASE_URL_MAINNET=
DATABASE_URL_TESTNET=

# Oracle publisher (scripts/oracle-keeper.ts). Run two processes, one key each,
# on separate hosts; the second with ORACLE_START_OFFSET_MS=500.
ORACLE_PUBLISHER_PRIVATE_KEY=
ORACLE_INTERVAL_MS=
ORACLE_START_OFFSET_MS=
ORACLE_PUSH_DEVIATION_BPS=
ORACLE_HEARTBEAT_SECS=
ORACLE_MIN_SOURCES=
ORACLE_MAX_SOURCE_DEVIATION_BPS=
ORACLE_SOURCES=
ORACLE_SOURCE_TIMEOUT_MS=
ORACLE_MAX_QUOTE_AGE_MS=
ORACLE_BACKDATE_SECS=
ORACLE_INCLUSION_MARGIN_SECS=
ORACLE_MAX_REF_DIVERGENCE_BPS=
ORACLE_ALERT_AFTER_SECS=
ORACLE_USDC_REFERENCE=
USDC_DEPEG_HALT_BPS=
USDC_DEPEG_FAIL_CLOSED=
