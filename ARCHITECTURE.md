# Architecture

The current architecture is specified in
[`docs/engineering/PROTOCOL_PLAN.md` §3](docs/engineering/PROTOCOL_PLAN.md#3-architecture):
the system diagram, the contract suite on Arc, the off-chain services and the
protocol invariants. Contract-level detail is in §4, fees in §5, the matching
engine and services in §6 and the oracle in §7.

This file previously described the legacy-chain system; it will be rewritten
here once the Arc services and app are built.
